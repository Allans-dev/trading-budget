const Landing = () => {
  return (
    <article style={styles.root}>
      <div>Hello World!</div>
    </article>
  );
};

const styles = {
  root: {
    gridArea: "Content",
    // paddingTop: "calc(40px+1em)",
  },
};

export default Landing;
